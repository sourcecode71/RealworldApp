@page "/listing"

<PageTitle>Listing</PageTitle>

 <section class="section">
      <div class="row">
        <div class="col-md-12 col-lg-12 col-sm-12">
		  <div class="card">
            <div class="card-body">
              <h5 class="card-title">Create Listing</h5>

			   <EditForm Model=@lgVM  OnInvalidSubmit=@InvalidFormSubmitted OnValidSubmit="@ValidFormSubmitted">
                <DataAnnotationsValidator />

                <div class="row mb-3">
                  <label for="inputText" class="col-sm-2 col-form-label">Title</label>
                  <div class="col-sm-10">
                      <InputText class="form-control" @bind-Value=@lgVM.Title />
                      <ValidationMessage For="@(()=>lgVM.Title)" />
                  </div>
                </div>

                <div class="row mb-3">
                  <label for="inputText" class="col-sm-2 col-form-label">Description</label>
                  <div class="col-sm-10">
                    <InputTextArea @bind-Value=lgVM.Description class="form-control" /> 
                    <ValidationMessage For="@(()=>lgVM.Description)" />
                  </div>
                </div>

                 <div class="row mb-3">
                     <div class="col-md-6 col-lg-6 col-sm-12">
                         <div class="row">
                             <label for="inputText" class="col-sm-4 col-form-label">Size</label>
                              <div class="col-sm-8">
                               <InputSelect @bind-Value=lgVM.Size class="form-select">
                                  <option value="Pending">Pending</option>
                                  <option value="Active">Active</option>
                                  <option value="Suspended">Suspended</option>
                                </InputSelect>
                                <ValidationMessage For="@(()=>lgVM.Size)" />
                              </div>
                           </div>
                     </div>

                     <div class="col-md-6 col-sm-12 col-lg-6">
                        <div class="row">
                          <label for="inputText" class="col-sm-4 col-form-label">Quantity</label>
                          <div class="col-sm-8">
                              <InputNumber @bind-Value=lgVM.Quantity class="form-control" />
                              <ValidationMessage For="@(()=>lgVM.Quantity)" />
                          </div>
                        </div>
                     </div>
              
                </div>
               
               <div class="mt-4">
                    <BrandingComponent bndVM=@bndVM @ref=@bndComponent />
                </div>

                 <div class="mt-4">
                    <PackagingComponent pkgVM=@pkgVM @ref=@pkgComponent />
                </div>

                <div class="row mb-3">
                    <div class=" col-2"></div>
                    <div class="col-sm-10">
                        <button type="submit" class="btn btn-primary">Submit </button>
                    </div>
                </div>

			   </EditForm>
            </div>
			</div>
		</div>
	  </div>
</section>


